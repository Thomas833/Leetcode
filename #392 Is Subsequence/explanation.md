I will use one pointer to walk along the main string from forward to backwards. I will also have a pointer along the substring and walk along it from beginning to end as well. i will compare the characters against each other and increment the main string each time and only increment the substring when the two characters match. i will end the loop when either pointer reaches the end and return false if the main string pointer 
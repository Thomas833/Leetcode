# #206 Reverse Linked List - Explanation

use a stack to store the values as you iterate over the linked list
pop from the stack as you create a new list and assign the values.


The time and space complexity is O(n)
